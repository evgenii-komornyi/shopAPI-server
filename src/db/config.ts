import { IConfig } from './IConfig.ts';

export const config: IConfig = {
    db: {
        host: 'localhost',
        user: 'evgenii',
        password: 'aeternitas666999',
        database: 'Shop',
        connectTimeout: 60000,
    },
};
