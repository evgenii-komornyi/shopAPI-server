export enum OrderStatus {
    CREATED = 1,
    AWAITING_PAYMENT = 2,
    AWAITING_SHIPMENT = 3,
    COMPLETED = 4,
    CANCELLED = 5,
    RETURNED = 6,
    REFUNDED = 7,
    FAILED = 8,
    SHIPPED = 9,
}
